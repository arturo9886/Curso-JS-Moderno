const producto = "monitor de 20 pulgadas";


/* lo que hacemos con .touppercase es que convertimos los strings a letras mayusculas
por ejemplo*/

console.log(producto.toUpperCase());

// para poder poner las letras en minusculas utilizamos .toolowercase EJM.

console.log(producto.toLowerCase());

/*to lowercase es importante ya que por ejemplo cuando usamos emails, hacemos que la base de datos siempre
obtenga la info de los emails en minusculas, porque no existen emails diferentes con mayusculas o minusculas. */