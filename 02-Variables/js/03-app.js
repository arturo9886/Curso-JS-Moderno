/*a las variables const, no se les puede cambiar el valor, ya
que toda su vida util quedara con ese valor especifico
*/

const adios = 'hola';
console.log (adios);

/*las variables con cost deben de tener un valor inicializado,
estas variables no se pueden inicializar depues*/

const holis = "holaaa"; 
console.log (holis);

/* lo mejor que se puede hacer es que si no tenemos el valor de la variable
o desconocemos de su valor o lo asignaremos despues, es usar let, y despues
si la conocemos y no la cambiaremos, usaremos const. */