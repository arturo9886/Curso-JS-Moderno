// crear arrays en javascript

//LOS ARRAYS AGRUPAN ELEMENTOS DEL MISMO TIPO
// un arreglo siempre va a contener informacion relacianada.
// los arreglos siempre se crean con corchetes.

const numeros = [10, 20, 30]

//miremos los objetos de nuevo, para ver la diferencia entre los dos.

const producto ={
    nombre: 'monitor de 20"',
    precio:300,
    disponible: true,
}

// como podemos ver, el objeto se conpone de llaves y valores de llaves, el array es simplemente una lista oa rreglo de elementos en comun.

//aqui hay optra manera de crear otro array.

const meses = new Array ('enero', 'febrero', 'marzo');

console.log (numeros);

// en al consola los arays, los lugares empiezan desde la posicion 0.

// en el arreglo no hay llaves ni valores, simplemente posiciones en el arreglo. 

// se puede crear un arreglo de multiples elementos, ademas de eso puedes crear arreglos con todo tipo de datos. 

// un arreglo con datos de todo tipo. 

const deTodo = ["hola", 10, true, 'si', null, {nombre: 'juan', trabajo: 'programador'}]

console.log (deTodo)

// basicamente un arreglo es una variable que contiene multiples elementos. 

// una lista de megusta, tambien una lista de amigos es un arreglo.

// ademas puede haber un arreglo dentro de un arreglo.